* лучше изучить static
* json сохранения
* камера
* музыка

* как реализовать плавное перемещение?
* разбитие глоб. переменных по файлам (переменные настройки графики, переменные для сцен...)
* имеет ли смысл добавлять отдельный коллайдер, чтобы необязательно было нажимать точечно на объект, чтобы его тригернуть?
* тайлсеты хранятся в excel файлах (чтобы динамически менять уровень):
каждая комната - отдельная сцена (с функцией on_tile_enter(int id_x, id_y) )
* при сохранении может хранится либо число, либо строка/название комнаты
мне нужна фабрика-функция, которая будет интерпретировать